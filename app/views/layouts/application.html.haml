!!!
%html
  %head
    %meta{:name => "viewport", :content => "width=device-width, initial-scale=1.0"}
    %title= content_for?(:title) ? yield(:title) : ENV['APP_NAME']
    %meta{:name => "description", :content => "#{content_for?(:description) ? yield(:description) : 'Ror Workshops'}"}
    = stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true
    = javascript_include_tag 'application', 'data-turbolinks-track' => true
    = csrf_meta_tags
  %body
    %header
      = render 'layouts/navigation'
    %main{:role => "main"}
      = render 'layouts/messages'
      .container-fluid
        %p.notice= notice
        %p.alert= alert
        .row
          .col-xs-10.col-xs-offset-1
            = breadcrumbs separator: " &raquo; "
            = yield
      .container-fluid
        .row
          .col-xs-10-xs-offset-1
            =link_to "br", "https://travis-ci.org/vento1982/netguru.svg?branch=master", :target => "_blank"
